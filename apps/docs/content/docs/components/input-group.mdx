---
title: Input Group
description: Combine inputs with addons like icons, text, buttons, and keyboard shortcuts.
---

## Installation

```bash
npm install @e412/rnui-react @e412/rnui-themes
```

## Import

```tsx
import {
  InputGroup,
  InputGroupAddon,
  InputGroupButton,
  InputGroupText,
  InputGroupInput,
  InputGroupTextarea,
} from '@e412/rnui-react'
```

## Usage

```tsx
import { InputGroup, InputGroupAddon, InputGroupInput } from '@e412/rnui-react'
import { SearchIcon } from 'lucide-react'

<InputGroup>
  <InputGroupAddon>
    <SearchIcon />
  </InputGroupAddon>
  <InputGroupInput placeholder="Search..." />
</InputGroup>
```

<Preview>
  <InputGroup className="max-w-sm">
    <InputGroupAddon>
      <InputGroupText>https://</InputGroupText>
    </InputGroupAddon>
    <InputGroupInput placeholder="example.com" />
  </InputGroup>
</Preview>

## Examples

### Icon Addon (Start)

```tsx
<InputGroup>
  <InputGroupAddon align="inline-start">
    <MailIcon />
  </InputGroupAddon>
  <InputGroupInput placeholder="Email" />
</InputGroup>
```

### Icon Addon (End)

```tsx
<InputGroup>
  <InputGroupInput placeholder="Search..." />
  <InputGroupAddon align="inline-end">
    <SearchIcon />
  </InputGroupAddon>
</InputGroup>
```

### Text Addon

```tsx
<InputGroup>
  <InputGroupAddon align="inline-start">
    <InputGroupText>https://</InputGroupText>
  </InputGroupAddon>
  <InputGroupInput placeholder="example.com" />
</InputGroup>
```

### Button Addon

```tsx
<InputGroup>
  <InputGroupInput placeholder="Search..." />
  <InputGroupAddon align="inline-end">
    <InputGroupButton>
      <SearchIcon />
    </InputGroupButton>
  </InputGroupAddon>
</InputGroup>
```

### Keyboard Shortcut Addon

```tsx
import { Kbd } from '@e412/rnui-react'

<InputGroup>
  <InputGroupInput placeholder="Search..." />
  <InputGroupAddon align="inline-end">
    <Kbd>⌘K</Kbd>
  </InputGroupAddon>
</InputGroup>
```

### Block Start Addon

```tsx
<InputGroup>
  <InputGroupAddon align="block-start">
    <InputGroupText>Title</InputGroupText>
  </InputGroupAddon>
  <InputGroupInput placeholder="Enter title..." />
</InputGroup>
```

### Block End Addon

```tsx
<InputGroup>
  <InputGroupInput placeholder="Enter message..." />
  <InputGroupAddon align="block-end">
    <InputGroupButton size="xs">Send</InputGroupButton>
  </InputGroupAddon>
</InputGroup>
```

### With Textarea

```tsx
<InputGroup>
  <InputGroupTextarea placeholder="Write a message..." />
  <InputGroupAddon align="block-end">
    <InputGroupButton size="xs">Submit</InputGroupButton>
  </InputGroupAddon>
</InputGroup>
```

## Props

### InputGroup

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `className` | `string` | - | Additional CSS class |

### InputGroupAddon

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `align` | `"inline-start" \| "inline-end" \| "block-start" \| "block-end"` | `"inline-start"` | Position of the addon |
| `className` | `string` | - | Additional CSS class |

### InputGroupButton

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `size` | `"xs" \| "sm" \| "icon-xs" \| "icon-sm"` | `"xs"` | Button size |
| `variant` | `string` | `"ghost"` | Button variant |
| `type` | `"button" \| "submit" \| "reset"` | `"button"` | Button type |
| `className` | `string` | - | Additional CSS class |

### InputGroupText

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `className` | `string` | - | Additional CSS class |

### InputGroupInput

Extends all standard `<input>` props. Renders a borderless `Input` within the group.

### InputGroupTextarea

Extends all standard `<textarea>` props. Renders a borderless `Textarea` within the group.

## Related

- [Input](/docs/components/input)
- [Button](/docs/components/button)
