---
title: Checkbox
description: A control that allows selecting one or more options.
---

import { Checkbox, Label } from '@e412/rnui-react';

The `Checkbox` component renders a styled checkbox input. Pair it with `Label` for accessible, clickable labels.

## Import

```tsx
import { Checkbox } from '@e412/rnui-react';
```

## Usage

<Preview code={`<div className="flex items-center gap-2">
  <Checkbox id="terms" />
  <Label htmlFor="terms">Accept terms and conditions</Label>
</div>`}>
  <div className="flex items-center gap-2">
    <Checkbox id="terms" />
    <Label htmlFor="terms">Accept terms and conditions</Label>
  </div>
</Preview>

## Checked by Default

<Preview code={`<div className="flex items-center gap-2">
  <Checkbox id="checked" defaultChecked />
  <Label htmlFor="checked">Checked by default</Label>
</div>`}>
  <div className="flex items-center gap-2">
    <Checkbox id="checked" defaultChecked />
    <Label htmlFor="checked">Checked by default</Label>
  </div>
</Preview>

## Disabled

<Preview code={`<div className="flex gap-6">
  <div className="flex items-center gap-2">
    <Checkbox id="disabled" disabled />
    <Label htmlFor="disabled">Disabled</Label>
  </div>
  <div className="flex items-center gap-2">
    <Checkbox id="disabled-checked" disabled defaultChecked />
    <Label htmlFor="disabled-checked">Disabled and checked</Label>
  </div>
</div>`}>
  <div className="flex gap-6">
    <div className="flex items-center gap-2">
      <Checkbox id="disabled" disabled />
      <Label htmlFor="disabled">Disabled</Label>
    </div>
    <div className="flex items-center gap-2">
      <Checkbox id="disabled-checked" disabled defaultChecked />
      <Label htmlFor="disabled-checked">Disabled and checked</Label>
    </div>
  </div>
</Preview>

## Examples

### Terms Agreement

<Preview code={`<div className="grid max-w-sm gap-4">
  <div className="flex items-start gap-2">
    <Checkbox id="agree-terms" className="mt-1" />
    <Label htmlFor="agree-terms" className="text-sm leading-relaxed">
      I agree to the Terms of Service and Privacy Policy. By checking this box, you confirm that you have read and understood our policies.
    </Label>
  </div>
</div>`}>
  <div className="grid max-w-sm gap-4">
    <div className="flex items-start gap-2">
      <Checkbox id="agree-terms" className="mt-1" />
      <Label htmlFor="agree-terms" className="text-sm leading-relaxed">
        I agree to the Terms of Service and Privacy Policy. By checking this box, you confirm that you have read and understood our policies.
      </Label>
    </div>
  </div>
</Preview>

### Multiple Checkboxes

<Preview code={`<div className="grid gap-3">
  <p className="text-sm font-medium">Select your interests:</p>
  <div className="flex items-center gap-2">
    <Checkbox id="interest-design" defaultChecked />
    <Label htmlFor="interest-design">Design</Label>
  </div>
  <div className="flex items-center gap-2">
    <Checkbox id="interest-dev" defaultChecked />
    <Label htmlFor="interest-dev">Development</Label>
  </div>
  <div className="flex items-center gap-2">
    <Checkbox id="interest-marketing" />
    <Label htmlFor="interest-marketing">Marketing</Label>
  </div>
  <div className="flex items-center gap-2">
    <Checkbox id="interest-sales" />
    <Label htmlFor="interest-sales">Sales</Label>
  </div>
</div>`}>
  <div className="grid gap-3">
    <p className="text-sm font-medium">Select your interests:</p>
    <div className="flex items-center gap-2">
      <Checkbox id="interest-design" defaultChecked />
      <Label htmlFor="interest-design">Design</Label>
    </div>
    <div className="flex items-center gap-2">
      <Checkbox id="interest-dev" defaultChecked />
      <Label htmlFor="interest-dev">Development</Label>
    </div>
    <div className="flex items-center gap-2">
      <Checkbox id="interest-marketing" />
      <Label htmlFor="interest-marketing">Marketing</Label>
    </div>
    <div className="flex items-center gap-2">
      <Checkbox id="interest-sales" />
      <Label htmlFor="interest-sales">Sales</Label>
    </div>
  </div>
</Preview>

### Notification Preferences

<Preview code={`<div className="grid max-w-sm gap-4">
  <p className="text-sm font-medium">Email notifications</p>
  <div className="grid gap-3">
    <div className="flex items-center justify-between">
      <Label htmlFor="notif-marketing">Marketing emails</Label>
      <Checkbox id="notif-marketing" />
    </div>
    <div className="flex items-center justify-between">
      <Label htmlFor="notif-security">Security alerts</Label>
      <Checkbox id="notif-security" defaultChecked />
    </div>
    <div className="flex items-center justify-between">
      <Label htmlFor="notif-updates">Product updates</Label>
      <Checkbox id="notif-updates" defaultChecked />
    </div>
  </div>
</div>`}>
  <div className="grid max-w-sm gap-4">
    <p className="text-sm font-medium">Email notifications</p>
    <div className="grid gap-3">
      <div className="flex items-center justify-between">
        <Label htmlFor="notif-marketing">Marketing emails</Label>
        <Checkbox id="notif-marketing" />
      </div>
      <div className="flex items-center justify-between">
        <Label htmlFor="notif-security">Security alerts</Label>
        <Checkbox id="notif-security" defaultChecked />
      </div>
      <div className="flex items-center justify-between">
        <Label htmlFor="notif-updates">Product updates</Label>
        <Checkbox id="notif-updates" defaultChecked />
      </div>
    </div>
  </div>
</Preview>
