# Docs App Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add an Astro + Starlight docs/example app at `apps/docs/` that showcases `@e412/rnui-react` components with live previews and code toggles.

**Architecture:** Starlight handles routing, sidebar, search, and dark mode. React component islands via `@astrojs/react` render live previews. Tailwind v4 via `@tailwindcss/vite` + `@astrojs/starlight-tailwind` brings rnui theme tokens into the docs. A reusable `ComponentPreview` React component wraps each example.

**Tech Stack:** Astro, @astrojs/starlight, @astrojs/react, @tailwindcss/vite, @astrojs/starlight-tailwind, @e412/rnui-react, @e412/rnui-themes

---

## File Structure

```
apps/
└── docs/
    ├── src/
    │   ├── components/
    │   │   └── component-preview.tsx
    │   ├── content/
    │   │   └── docs/
    │   │       ├── index.mdx
    │   │       ├── getting-started.mdx
    │   │       ├── theming.mdx
    │   │       └── components/
    │   │           ├── button.mdx
    │   │           ├── card.mdx
    │   │           └── dialog.mdx
    │   ├── content.config.ts
    │   └── styles/
    │       └── global.css
    ├── astro.config.mjs
    ├── package.json
    └── tsconfig.json
```

---

### Task 1: Update Workspace and Scaffold Astro App

**Files:**
- Modify: `pnpm-workspace.yaml`
- Create: `apps/docs/package.json`
- Create: `apps/docs/tsconfig.json`
- Create: `apps/docs/astro.config.mjs`
- Create: `apps/docs/src/content.config.ts`
- Create: `apps/docs/src/content/docs/index.mdx`
- Create: `apps/docs/src/styles/global.css`

- [ ] **Step 1: Update pnpm-workspace.yaml**

Edit `/home/aloks98/projects/rnui/pnpm-workspace.yaml` to:

```yaml
packages:
  - "packages/*"
  - "apps/*"
```

- [ ] **Step 2: Create apps/docs directory structure**

```bash
mkdir -p apps/docs/src/{components,content/docs/components,styles}
```

- [ ] **Step 3: Create apps/docs/package.json**

Create `apps/docs/package.json`:

```json
{
  "name": "@e412/docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/react": "^4.0.0",
    "@astrojs/starlight": "^0.38.0",
    "@astrojs/starlight-tailwind": "^3.0.0",
    "@e412/rnui-react": "workspace:*",
    "@e412/rnui-themes": "workspace:*",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^5.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
```

- [ ] **Step 4: Create apps/docs/tsconfig.json**

Create `apps/docs/tsconfig.json`:

```json
{
  "extends": "astro/tsconfigs/strict"
}
```

- [ ] **Step 5: Create apps/docs/astro.config.mjs**

Create `apps/docs/astro.config.mjs`:

```js
import { defineConfig } from 'astro/config'
import starlight from '@astrojs/starlight'
import react from '@astrojs/react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  integrations: [
    starlight({
      title: '@e412/rnui',
      sidebar: [
        { label: 'Getting Started', slug: 'getting-started' },
        { label: 'Theming', slug: 'theming' },
        {
          label: 'Components',
          items: [
            { label: 'Button', slug: 'components/button' },
            { label: 'Card', slug: 'components/card' },
            { label: 'Dialog', slug: 'components/dialog' },
          ],
        },
      ],
      customCss: ['./src/styles/global.css'],
    }),
    react(),
  ],
  vite: {
    plugins: [tailwindcss()],
  },
})
```

- [ ] **Step 6: Create apps/docs/src/content.config.ts**

Create `apps/docs/src/content.config.ts`:

```ts
import { defineCollection } from 'astro:content'
import { docsLoader } from '@astrojs/starlight/loaders'
import { docsSchema } from '@astrojs/starlight/schema'

export const collections = {
  docs: defineCollection({
    loader: docsLoader(),
    schema: docsSchema(),
  }),
}
```

- [ ] **Step 7: Create apps/docs/src/styles/global.css**

Create `apps/docs/src/styles/global.css`:

```css
@layer base, starlight, theme, components, utilities;
@import '@astrojs/starlight-tailwind';
@import 'tailwindcss/theme.css' layer(theme);
@import 'tailwindcss/utilities.css' layer(utilities);
@import '@e412/rnui-themes';
```

This sets up cascade layers for Starlight + Tailwind v4 compatibility, then imports the rnui theme tokens.

- [ ] **Step 8: Create apps/docs/src/content/docs/index.mdx**

Create `apps/docs/src/content/docs/index.mdx`:

```mdx
---
title: '@e412/rnui'
description: A shared UI component library built on shadcn/ui.
template: splash
hero:
  title: '@e412/rnui'
  tagline: A shared UI component library built on shadcn/ui. Ships React components and prebuilt themes.
  actions:
    - text: Get Started
      link: /getting-started/
      icon: right-arrow
---
```

- [ ] **Step 9: Install dependencies**

```bash
cd /home/aloks98/projects/rnui
pnpm install
```

- [ ] **Step 10: Verify the dev server starts**

```bash
cd /home/aloks98/projects/rnui/apps/docs
pnpm dev
```

Expected: Astro dev server starts, landing page renders at `http://localhost:4321/`.

Press Ctrl+C to stop after verifying.

- [ ] **Step 11: Commit**

```bash
cd /home/aloks98/projects/rnui
git add pnpm-workspace.yaml apps/docs/ pnpm-lock.yaml
git commit -m "feat: scaffold astro starlight docs app"
```

---

### Task 2: Create ComponentPreview Component

**Files:**
- Create: `apps/docs/src/components/component-preview.tsx`

- [ ] **Step 1: Create apps/docs/src/components/component-preview.tsx**

Create `apps/docs/src/components/component-preview.tsx`:

```tsx
import { useState, type ReactNode } from 'react'

interface ComponentPreviewProps {
  children: ReactNode
  code: string
}

export function ComponentPreview({ children, code }: ComponentPreviewProps) {
  const [showCode, setShowCode] = useState(false)

  return (
    <div className="not-content my-6">
      <div className="flex min-h-[150px] items-center justify-center rounded-t-lg border border-border bg-background p-6">
        {children}
      </div>
      <div className="border-x border-b border-border rounded-b-lg overflow-hidden">
        <button
          type="button"
          onClick={() => setShowCode(!showCode)}
          className="flex w-full items-center justify-center gap-2 border-b border-border bg-muted/50 px-4 py-2 text-sm text-muted-foreground hover:bg-muted transition-colors"
        >
          {showCode ? 'Hide Code' : 'Show Code'}
        </button>
        {showCode && (
          <pre className="overflow-x-auto p-4 text-sm bg-muted/30">
            <code>{code}</code>
          </pre>
        )}
      </div>
    </div>
  )
}
```

Key decisions:
- `not-content` class prevents Starlight from applying its prose styles inside the preview
- Uses rnui theme tokens (`border-border`, `bg-background`, `text-muted-foreground`, etc.) so it adapts to light/dark mode
- Simple `useState` toggle for code visibility
- No syntax highlighting for now — keeps it simple, can add later

- [ ] **Step 2: Verify it builds**

```bash
cd /home/aloks98/projects/rnui/apps/docs
pnpm build
```

Expected: Build succeeds (component isn't used yet, but should compile).

- [ ] **Step 3: Commit**

```bash
cd /home/aloks98/projects/rnui
git add apps/docs/src/components/component-preview.tsx
git commit -m "feat: add ComponentPreview component for live examples"
```

---

### Task 3: Create Getting Started Page

**Files:**
- Create: `apps/docs/src/content/docs/getting-started.mdx`

- [ ] **Step 1: Create apps/docs/src/content/docs/getting-started.mdx**

Create `apps/docs/src/content/docs/getting-started.mdx`:

```mdx
---
title: Getting Started
description: Install and set up @e412/rnui in your project.
---

import { Button } from '@e412/rnui-react'

## Installation

Install the React components and themes packages:

```bash
npm install @e412/rnui-react @e412/rnui-themes
```

Or with pnpm:

```bash
pnpm add @e412/rnui-react @e412/rnui-themes
```

## CSS Setup

Import the theme CSS in your app's global stylesheet:

```css
@import '@e412/rnui-themes';
@import 'tw-animate-css';
```

## Tailwind v4 Integration

The theme CSS includes a `@theme inline` block that automatically registers all design tokens with Tailwind v4. Just importing `@e412/rnui-themes` gives you utilities like `bg-primary`, `text-muted-foreground`, etc.

Add the library to your Tailwind content sources:

```css
@source "../node_modules/@e412/rnui-react/dist";
```

## Dark Mode

Toggle dark mode by adding the `dark` class to your `<html>` element:

```html
<html class="dark">
```

The theme automatically switches all color tokens.

## Quick Example

Once installed, import and use components:

```tsx
import { Button } from '@e412/rnui-react'

export function MyApp() {
  return <Button>Click me</Button>
}
```

Here's a live preview:

<Button client:load>Click me</Button>
```

- [ ] **Step 2: Verify the dev server renders the page**

```bash
cd /home/aloks98/projects/rnui/apps/docs
pnpm dev
```

Navigate to `http://localhost:4321/getting-started/`. Expected: Page renders with content. The live Button should be interactive.

Press Ctrl+C to stop.

- [ ] **Step 3: Commit**

```bash
cd /home/aloks98/projects/rnui
git add apps/docs/src/content/docs/getting-started.mdx
git commit -m "docs: add getting started page"
```

---

### Task 4: Create Theming Page

**Files:**
- Create: `apps/docs/src/content/docs/theming.mdx`

- [ ] **Step 1: Create apps/docs/src/content/docs/theming.mdx**

Create `apps/docs/src/content/docs/theming.mdx`:

```mdx
---
title: Theming
description: How the @e412/rnui theme system works.
---

## How It Works

The theme system uses **CSS custom properties** (variables) with **oklch** color values. Two packages work together:

- **@e412/rnui-themes** — defines all CSS variables and a Tailwind v4 `@theme inline` block
- **@e412/rnui-react** — components reference these variables via Tailwind utility classes

## Available Tokens

### Colors

| Token | Description |
|-------|-------------|
| `--background` / `--foreground` | Page background and text |
| `--card` / `--card-foreground` | Card surfaces |
| `--popover` / `--popover-foreground` | Popover/dropdown surfaces |
| `--primary` / `--primary-foreground` | Primary action color |
| `--secondary` / `--secondary-foreground` | Secondary action color |
| `--muted` / `--muted-foreground` | Muted/disabled elements |
| `--accent` / `--accent-foreground` | Accent highlights |
| `--destructive` / `--destructive-foreground` | Destructive/danger actions |
| `--border` | Default border color |
| `--input` | Input border color |
| `--ring` | Focus ring color |
| `--chart-1` through `--chart-5` | Chart color palette |
| `--sidebar-*` | Sidebar-specific variants |

### Border Radius

| Token | Value |
|-------|-------|
| `--radius` | Base radius (`0.625rem`) |
| `--radius-sm` | `calc(var(--radius) - 4px)` |
| `--radius-md` | `calc(var(--radius) - 2px)` |
| `--radius-lg` | `var(--radius)` |
| `--radius-xl` | `calc(var(--radius) + 4px)` |

## Light and Dark Mode

The light theme is applied on `:root`. The dark theme activates when the `.dark` class is on the `<html>` element:

```css
/* Light — active by default */
:root {
  --background: 1 0 0;
  --foreground: 0.145 0 0;
}

/* Dark — activated by .dark class */
.dark {
  --background: 0.145 0 0;
  --foreground: 0.985 0 0;
}
```

## Custom Themes

Create a custom theme by overriding the CSS variables under a custom class:

```css
.theme-ocean {
  --background: 0.97 0.01 230;
  --foreground: 0.2 0.02 230;
  --primary: 0.55 0.2 250;
  --primary-foreground: 0.98 0 0;
  /* ... override other tokens as needed */
}
```

Apply it by adding the class to `<html>`:

```html
<html class="theme-ocean">
```

## Tailwind v4 Integration

The `@theme inline` block in `@e412/rnui-themes` maps all CSS variables to Tailwind utilities:

```css
@theme inline {
  --color-primary: oklch(var(--primary));
  --color-background: oklch(var(--background));
  /* ... all tokens mapped */
}
```

This means `bg-primary`, `text-foreground`, `border-border`, etc. all work out of the box.
```

- [ ] **Step 2: Verify the page renders**

```bash
cd /home/aloks98/projects/rnui/apps/docs
pnpm dev
```

Navigate to `http://localhost:4321/theming/`. Expected: Page renders with tables and code blocks.

Press Ctrl+C to stop.

- [ ] **Step 3: Commit**

```bash
cd /home/aloks98/projects/rnui
git add apps/docs/src/content/docs/theming.mdx
git commit -m "docs: add theming guide page"
```

---

### Task 5: Create Button Component Page

**Files:**
- Create: `apps/docs/src/content/docs/components/button.mdx`

- [ ] **Step 1: Create apps/docs/src/content/docs/components/button.mdx**

Create `apps/docs/src/content/docs/components/button.mdx`:

```mdx
---
title: Button
description: A button component with multiple variants and sizes.
---

import { ComponentPreview } from '../../../components/component-preview'
import { Button } from '@e412/rnui-react'

## Usage

```tsx
import { Button } from '@e412/rnui-react'
```

## Default

<ComponentPreview client:load code={`<Button>Button</Button>`}>
  <Button>Button</Button>
</ComponentPreview>

## Variants

### Secondary

<ComponentPreview client:load code={`<Button variant="secondary">Secondary</Button>`}>
  <Button variant="secondary">Secondary</Button>
</ComponentPreview>

### Destructive

<ComponentPreview client:load code={`<Button variant="destructive">Destructive</Button>`}>
  <Button variant="destructive">Destructive</Button>
</ComponentPreview>

### Outline

<ComponentPreview client:load code={`<Button variant="outline">Outline</Button>`}>
  <Button variant="outline">Outline</Button>
</ComponentPreview>

### Ghost

<ComponentPreview client:load code={`<Button variant="ghost">Ghost</Button>`}>
  <Button variant="ghost">Ghost</Button>
</ComponentPreview>

### Link

<ComponentPreview client:load code={`<Button variant="link">Link</Button>`}>
  <Button variant="link">Link</Button>
</ComponentPreview>

## Sizes

<ComponentPreview client:load code={`<div className="flex items-center gap-4">
  <Button size="sm">Small</Button>
  <Button size="default">Default</Button>
  <Button size="lg">Large</Button>
</div>`}>
  <div className="flex items-center gap-4">
    <Button size="sm">Small</Button>
    <Button size="default">Default</Button>
    <Button size="lg">Large</Button>
  </div>
</ComponentPreview>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `"default" \| "secondary" \| "destructive" \| "outline" \| "ghost" \| "link"` | `"default"` | Visual style variant |
| `size` | `"default" \| "sm" \| "lg" \| "icon"` | `"default"` | Button size |
| `asChild` | `boolean` | `false` | Render as child element |
| `disabled` | `boolean` | `false` | Disabled state |
```

- [ ] **Step 2: Verify the page renders with live previews**

```bash
cd /home/aloks98/projects/rnui/apps/docs
pnpm dev
```

Navigate to `http://localhost:4321/components/button/`. Expected: Page renders with live interactive Button previews and code toggles.

Press Ctrl+C to stop.

- [ ] **Step 3: Commit**

```bash
cd /home/aloks98/projects/rnui
git add apps/docs/src/content/docs/components/button.mdx
git commit -m "docs: add button component page with live previews"
```

---

### Task 6: Create Card Component Page

**Files:**
- Create: `apps/docs/src/content/docs/components/card.mdx`

- [ ] **Step 1: Create apps/docs/src/content/docs/components/card.mdx**

Create `apps/docs/src/content/docs/components/card.mdx`:

```mdx
---
title: Card
description: A container component for grouping related content.
---

import { ComponentPreview } from '../../../components/component-preview'
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '@e412/rnui-react'
import { Button } from '@e412/rnui-react'

## Usage

```tsx
import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
} from '@e412/rnui-react'
```

## Default

<ComponentPreview client:load code={`<Card className="w-[350px]">
  <CardHeader>
    <CardTitle>Card Title</CardTitle>
    <CardDescription>Card description goes here.</CardDescription>
  </CardHeader>
  <CardContent>
    <p>Card content goes here.</p>
  </CardContent>
  <CardFooter>
    <Button>Action</Button>
  </CardFooter>
</Card>`}>
  <Card className="w-[350px]">
    <CardHeader>
      <CardTitle>Card Title</CardTitle>
      <CardDescription>Card description goes here.</CardDescription>
    </CardHeader>
    <CardContent>
      <p>Card content goes here.</p>
    </CardContent>
    <CardFooter>
      <Button>Action</Button>
    </CardFooter>
  </Card>
</ComponentPreview>

## Simple Card

<ComponentPreview client:load code={`<Card className="w-[350px]">
  <CardContent className="pt-6">
    <p>A simple card with just content.</p>
  </CardContent>
</Card>`}>
  <Card className="w-[350px]">
    <CardContent className="pt-6">
      <p>A simple card with just content.</p>
    </CardContent>
  </Card>
</ComponentPreview>

## Props

### Card

| Prop | Type | Description |
|------|------|-------------|
| `className` | `string` | Additional CSS classes |
| `children` | `ReactNode` | Card contents |

### CardHeader, CardContent, CardFooter

| Prop | Type | Description |
|------|------|-------------|
| `className` | `string` | Additional CSS classes |
| `children` | `ReactNode` | Section contents |

### CardTitle

| Prop | Type | Description |
|------|------|-------------|
| `className` | `string` | Additional CSS classes |
| `children` | `ReactNode` | Title text |

### CardDescription

| Prop | Type | Description |
|------|------|-------------|
| `className` | `string` | Additional CSS classes |
| `children` | `ReactNode` | Description text |
```

- [ ] **Step 2: Verify the page renders**

```bash
cd /home/aloks98/projects/rnui/apps/docs
pnpm dev
```

Navigate to `http://localhost:4321/components/card/`. Expected: Page renders with live Card previews.

Press Ctrl+C to stop.

- [ ] **Step 3: Commit**

```bash
cd /home/aloks98/projects/rnui
git add apps/docs/src/content/docs/components/card.mdx
git commit -m "docs: add card component page with live previews"
```

---

### Task 7: Create Dialog Component Page

**Files:**
- Create: `apps/docs/src/content/docs/components/dialog.mdx`

- [ ] **Step 1: Create apps/docs/src/content/docs/components/dialog.mdx**

Create `apps/docs/src/content/docs/components/dialog.mdx`:

```mdx
---
title: Dialog
description: A modal dialog component for focused interactions.
---

import { ComponentPreview } from '../../../components/component-preview'
import { Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter } from '@e412/rnui-react'
import { Button } from '@e412/rnui-react'

## Usage

```tsx
import {
  Dialog,
  DialogTrigger,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogDescription,
  DialogFooter,
} from '@e412/rnui-react'
```

## Default

<ComponentPreview client:load code={`<Dialog>
  <DialogTrigger asChild>
    <Button variant="outline">Open Dialog</Button>
  </DialogTrigger>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Dialog Title</DialogTitle>
      <DialogDescription>
        This is a description of the dialog content.
      </DialogDescription>
    </DialogHeader>
    <div className="py-4">
      <p>Dialog body content goes here.</p>
    </div>
    <DialogFooter>
      <Button variant="outline">Cancel</Button>
      <Button>Confirm</Button>
    </DialogFooter>
  </DialogContent>
</Dialog>`}>
  <Dialog>
    <DialogTrigger asChild>
      <Button variant="outline">Open Dialog</Button>
    </DialogTrigger>
    <DialogContent>
      <DialogHeader>
        <DialogTitle>Dialog Title</DialogTitle>
        <DialogDescription>
          This is a description of the dialog content.
        </DialogDescription>
      </DialogHeader>
      <div className="py-4">
        <p>Dialog body content goes here.</p>
      </div>
      <DialogFooter>
        <Button variant="outline">Cancel</Button>
        <Button>Confirm</Button>
      </DialogFooter>
    </DialogContent>
  </Dialog>
</ComponentPreview>

## Props

### Dialog

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `open` | `boolean` | — | Controlled open state |
| `onOpenChange` | `(open: boolean) => void` | — | Callback when open state changes |
| `defaultOpen` | `boolean` | `false` | Initial open state (uncontrolled) |

### DialogTrigger

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `asChild` | `boolean` | `false` | Render as child element |

### DialogContent

| Prop | Type | Description |
|------|------|-------------|
| `className` | `string` | Additional CSS classes |
| `children` | `ReactNode` | Dialog contents |

### DialogHeader, DialogFooter

| Prop | Type | Description |
|------|------|-------------|
| `className` | `string` | Additional CSS classes |
| `children` | `ReactNode` | Section contents |

### DialogTitle, DialogDescription

| Prop | Type | Description |
|------|------|-------------|
| `className` | `string` | Additional CSS classes |
| `children` | `ReactNode` | Text content |
```

- [ ] **Step 2: Verify the page renders**

```bash
cd /home/aloks98/projects/rnui/apps/docs
pnpm dev
```

Navigate to `http://localhost:4321/components/dialog/`. Expected: Page renders. Clicking "Open Dialog" opens a modal.

Press Ctrl+C to stop.

- [ ] **Step 3: Commit**

```bash
cd /home/aloks98/projects/rnui
git add apps/docs/src/content/docs/components/dialog.mdx
git commit -m "docs: add dialog component page with live previews"
```

---

### Task 8: Full Build and Verification

**Files:**
- No new files — validation only

- [ ] **Step 1: Build the entire monorepo**

```bash
cd /home/aloks98/projects/rnui
pnpm build
```

Expected: Themes builds, React builds, then Astro docs builds. No errors.

Note: The root `pnpm build` script only builds packages. Run the docs build separately:

```bash
pnpm --filter @e412/docs build
```

Expected: Astro static build succeeds. Output in `apps/docs/dist/`.

- [ ] **Step 2: Preview the built site**

```bash
cd /home/aloks98/projects/rnui/apps/docs
pnpm preview
```

Navigate to `http://localhost:4321/`. Verify:
- Landing page renders
- Sidebar shows all pages
- Getting Started page has content
- Theming page has tables and code blocks
- Button page shows live interactive previews with code toggles
- Card page shows live previews
- Dialog page — clicking "Open Dialog" opens a modal
- Dark mode toggle works and components switch themes

Press Ctrl+C to stop.

- [ ] **Step 3: Add docs build to root package.json**

Edit `/home/aloks98/projects/rnui/package.json` to add a `docs:dev` and `docs:build` script:

Add to the `"scripts"` object:

```json
"docs:dev": "pnpm --filter @e412/docs dev",
"docs:build": "pnpm --filter @e412/docs build"
```

- [ ] **Step 4: Commit**

```bash
cd /home/aloks98/projects/rnui
git add apps/docs/ package.json
git commit -m "chore: verify docs build and add root docs scripts"
```
